<!--菜单栏高亮-->
<script type="text/javascript">menu_active="060"</script>

<h5>原生对象函数</h5>

<p>Def 的基本类型、容器类型与高级类型变量自带了一些函数，可以方便的调用对数据进行操作。</p>

<p>由点号<code>.</code>访问，使用方式如下：</p>

<code class="block"><span># 原生对象函数</span>
sss : <u>"123 abc xyz"</u>
arr : sss.<i>split</i>(' ')  <span># 分割字符串</span>
<i>print</i>(arr)  <span># ('123' 'abc' 'xyz')</span>
</code>