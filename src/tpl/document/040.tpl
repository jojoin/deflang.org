<!--菜单栏高亮-->
<script type="text/javascript">menu_active="040"</script>

<h6>控制结构</h6>

<p>Def 语言内置了条件分支、循环、遍历等常用的程序设计结构，并精简了非必要的结构符号，代码简捷紧凑高效。</p>

<p>所有控制结构采用统一的定义模式：<code><关键字></code> + <code>;</code>。实际上函数和控制器以及类定义也是一种控制结构，但由于涉及内容较多，将专门新启章节说明。</p>

<p>注意：一定不要忘了末尾的分号<code>;</code>！</p>
